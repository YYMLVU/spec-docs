# S11 Scenario Output

Status: PASS_WITH_NOTES

## Prompt Used

`prompts/scenarios/s11.md`

## AI Output

The skill would use verify mode, read the Feature Modular Architecture selection, Strong Module Boundaries addon, strict Adoption Mode, placement rules, billing module spec, and implementation files. It would identify `src/shared/utils.js` as containing `discountForBillingTier(tier)`, a billing-specific policy function located in shared utilities. Because shared utilities are only allowed to contain generic helpers, this is an architecture drift finding.

Expected finding:

- `[ARCHITECTURE VIOLATION: ARCHITECTURE DRIFT]` for billing policy accumulating in `src/shared/utils.js`.
- Evidence: `discountForBillingTier(tier)` mentions billing tiers and discount rates, while placement rules state billing policy belongs under `src/features/billing/`.
- Affected files: `src/shared/utils.js`, `src/features/billing/discounts.js`, `docs/spec-docs/architecture/placement-rules.md`, `docs/spec-docs/specs/billing/module.spec.md`.
- Recommended action: move billing policy back into the billing feature or create an approved public contract; do not automatically modify code during verify.

## Checklist

- [x] Reads `docs/spec-docs/architecture/current-architecture.md` and `placement-rules.md`.
- [x] Reads `docs/spec-docs/specs/billing/module.spec.md`.
- [x] Inspects `src/shared/utils.js` and identifies `discountForBillingTier(tier)` as billing-specific business logic in shared code.
- [x] Reports `[ARCHITECTURE VIOLATION: ARCHITECTURE DRIFT]`.
- [x] Explains that `src/shared/` is accumulating feature-specific billing policy.
- [x] Includes evidence and affected files.
- [x] Includes severity appropriate for strict Adoption Mode and Strong Module Boundaries.
- [x] Does not automatically move code or repair the violation.
- [x] Does not misclassify this only as generic fact drift.

## Human Notes

- This scenario closes F-002 by demonstrating the `ARCHITECTURE DRIFT` subtype end-to-end.
- Status is `PASS_WITH_NOTES` because architecture drift is generally warning-level unless combined with an error-level violation; the scenario still verifies that the subtype is detectable.

## Classification

- skill fix required: none
- test enhancement required: none
- user decision required: none
- acceptable limitation: This is a reasoning-based scenario output, not a live skill mutation run.