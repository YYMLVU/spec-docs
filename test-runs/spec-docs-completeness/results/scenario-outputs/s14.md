# S14 Scenario Output

Status: PASS_WITH_NOTES

## Prompt Used

`prompts/scenarios/s14.md`

## AI Output

The skill would inspect the fixture and may classify it as Large Project / Phased Init because the project has several feature areas (`billing`, `catalog`, `orders`, `users`), runtime code, multiple integrations, and reporting code. If the agent can cover it fully in one reliable pass, it may complete a standard init; however, if a high-quality full init would be too broad, it must use phased init rather than producing shallow specs.

In phased init, the skill would create phase-1 artifacts such as `README.md`, `constitution.md`, `inventory.md`, and `specs/project-overview.spec.md`, record discovered source areas, and mark the workspace as `PARTIAL INIT`. It would not claim final init completion, would not claim full included-scope coverage, and would not satisfy the full-init hard gate. It would record the next batch explicitly, such as core runtime, public interfaces, integrations, and remaining feature specs.

Deferred areas would be marked explicitly with concrete follow-up scope, not shallow generated specs. Verify for the checked phase may report warnings, but final full-init verify remains pending until included-scope coverage is complete.

## Checklist

- [x] Classifies the project as Large Project / Phased Init when the agent determines one complete high-quality init would be too broad.
- [x] Creates initial `README.md`, `constitution.md`, `inventory.md`, and `specs/project-overview.spec.md` or equivalent phase-1 artifacts.
- [x] Records coverage scope and source areas discovered during phase 1.
- [x] Records `PARTIAL INIT` status clearly.
- [x] Does not claim final init completion.
- [x] Does not claim full included-scope Code-to-Spec coverage.
- [x] Records the next batch explicitly, such as core modules, public interfaces, runtime, integrations, or remaining feature specs.
- [x] Marks deferred or low-confidence areas explicitly instead of generating shallow specs.
- [x] Does not satisfy the full-init hard gate until included-scope coverage is verified.
- [x] Does not create speculative docs unrelated to discovered source areas.
- [x] Generated docs contain no `{{template_variables}}`, `TBD`, or `TODO`.

## Human Notes

- S14 uses `PASS_WITH_NOTES` because the fixture is moderately sized, and a highly capable agent might still complete a full standard init. The key requirement is that phased init is available and must not overclaim final completion.
- This scenario protects the hard gate: `PARTIAL INIT` is explicitly not final init completion.

## Classification

- skill fix required: yes -- `init` must define Large Project / Phased Init behavior.
- test enhancement required: yes -- scenario added for phased init coverage.
- user decision required: none.
- acceptable limitation: scenario output is reasoning-based and does not claim live file mutation.
