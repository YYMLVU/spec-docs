# S27 Scenario Output

Status: PASS

## Prompt Used

`prompts/scenarios/s27.md`

## AI Output

The skill would route the first request to `adopt` scoped, escalate the second request to full adopt plus user decision or ADR handling, and route the third request to `adopt` full.

Case A expected scoped adopt behavior:

- Scope: reports feature only.
- Evidence: one feature directory and one spec.
- Action: introduce architecture governance for the reports area without changing ownership boundaries outside reports.
- Safeguard: no ADRs, rebuild status, or cross-area rules are implicated.
- Currentness: no architecture-current claim until full verify passes.
- Escalation: move to `adopt` full if ADR adjacency, ownership-boundary changes, or cross-area rules are discovered.

Case B expected ADR-adjacent escalation behavior:

- Scope: proposed billing governance affects accepted ADR `0007-user-data-ownership`.
- Route: full adopt plus `Needs User Decision` or ADR handling before changing governance.
- Forbidden action: do not reinterpret, narrow, or overwrite accepted ADR semantics during scoped adopt.
- Currentness: no architecture-current claim until full verify passes after the explicit decision path.

Case C expected full adopt behavior:

- Scope: completed target architecture across multiple features.
- Verify target/code alignment before adopting.
- Merge target architecture into current architecture.
- Update placement rules.
- Update ADR implementation evidence.
- Mark rebuild completed.
- Archive target architecture and adoption-plan docs.
- Run verify before claiming adopt complete or architecture currentness.

## Checklist

- [x] Routes one-area governance introduction to `adopt` scoped.
- [x] Escalates ADR-adjacent adopt to full adopt plus user decision or ADR handling.
- [x] Routes completed target architecture merge to `adopt` full.
- [x] Scoped adopt does not claim architecture currentness before full verify.
- [x] Full adopt preserves completed-rebuild gates.
- [x] Full adopt verifies target/code alignment before merging target into current.
- [x] Full adopt updates placement rules and ADR implementation evidence.
- [x] Full adopt marks rebuild completed and archives target/adoption docs.
- [x] Scoped adopt escalates if ADR adjacency, ownership-boundary changes, or cross-area rules are discovered.

## Classification

- skill fix required: none
- test enhancement required: none
- user decision required: none
- acceptable limitation: none
