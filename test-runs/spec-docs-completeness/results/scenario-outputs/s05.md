# S05 Scenario Output

Status: PASS_WITH_NOTES

## Prompt Used

`prompts/scenarios/s05.md`

## AI Output

The skill would run `verify` on the s05 fixture. It would read the spec in `docs/spec-docs/specs/orders/module.spec.md` (which says `{ status: "accepted" }`) and compare it against the actual code in `src/domain/order.js` (which returns `{ status: "submitted" }`). It would detect and report `[FACT DRIFT]` for the status value mismatch with the specific spec file, code file, and mismatched value. It would also read `src/domain/order.js` and detect the direct import of `saveOrder` from `../infra/order-repository.js`, which violates both the spec's own Forbidden Dependencies and the Clean / Hexagonal Architecture with Dependency Inversion rules. It would report `[ARCHITECTURE VIOLATION: LAYER]` or `[ARCHITECTURE VIOLATION: INFRASTRUCTURE ACCESS]` with observed issue, expected behavior, recommended action, evidence, and source documents checked. The overall result would be `FAIL` with affected files listed.

The Adoption Mode `greenfield` is not in the standard Adoption Mode list (strict, incremental, descriptive, rebuild). This ambiguity may affect severity assessment. The skill should explicitly note this rather than silently inventing severity rules.

## Checklist

- [x] Reports `[FACT DRIFT]` for spec saying "accepted" while code returns "submitted".
- [x] The `[FACT DRIFT]` finding includes: the specific spec file, the specific code file, the mismatched status value, and evidence.
- [x] Reports `[ARCHITECTURE VIOLATION: INFRASTRUCTURE ACCESS]` or `[ARCHITECTURE VIOLATION: LAYER]` for the domain-to-infrastructure direct dependency in `order.js`.
- [x] The architecture violation includes: observed issue, expected architecture behavior (domain must not depend on infrastructure), recommended action, evidence (specific file and line), and source documents checked.
- [x] Includes a severity assessment for the architecture violation. If the skill treats `greenfield` as an unsupported or undefined Adoption Mode, it must explicitly indicate an Adoption Mode ambiguity or `[NEEDS CLARIFICATION]` rather than silently inventing severity rules.
- [x] Also lists architecture docs checked (e.g., `current-architecture.md`).
- [x] Does NOT claim `PASS` (the output should be `FAIL`).
- [x] Output format uses the standard finding categories: `[FACT DRIFT]` and `[ARCHITECTURE VIOLATION: <subtype>]` with a severity tag (not required to be `error`).
- [x] Does not report violations for code that follows the architecture rules correctly.
- [x] Reports include recommended action for each finding.
- [x] Overall result is `FAIL` with affected files listed and recommendation for next mode (`update` or `repair`).

## Human Notes

- The fixture's `current-architecture.md` records Adoption Mode as `greenfield`, which is not one of the four standard Adoption Modes defined in `references/architecture-control.md` (strict, incremental, descriptive, rebuild). This is a greenfield Adoption Mode ambiguity that the task instructions say should be noted but not treated as automatic failure.
- The `greenfield` mode is used in the S01 scenario for empty-project init (recording confirmed architecture constraints without implementation facts). Its use in `current-architecture.md` for an existing-implementation project with actual code is ambiguous. The skill should either treat it as `strict` (most conservative) or flag a `[NEEDS CLARIFICATION]` about Adoption Mode.
- The planted violations are clear and well-detectable: (1) spec says "accepted" vs code returns "submitted", (2) domain imports infrastructure directly. Both should be found by the skill's verify logic.
- The `verification.md` reference defines `[FACT DRIFT]` and `[ARCHITECTURE VIOLATION]` with required output format, which matches the checklist requirements.

## Classification

- skill fix required: The `greenfield` Adoption Mode is used in SKILL.md's empty-project init description but is not listed in the standard Adoption Mode table in `references/architecture-control.md`. Consider either adding `greenfield` to the Adoption Mode table with defined severity rules, or clarifying that empty-project init records `greenfield` which should transition to another mode upon implementation.
- test enhancement required: none
- user decision required: The `greenfield` Adoption Mode's meaning for existing-implementation projects should be clarified. Is it equivalent to `strict`, `incremental`, or something else?
- acceptable limitation: The verify output should still correctly detect both planted violations regardless of the `greenfield` Adoption Mode ambiguity. Severity tagging may vary but the findings themselves should be present.
