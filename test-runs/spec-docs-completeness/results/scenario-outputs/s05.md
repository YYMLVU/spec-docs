# S05 Scenario Output

Status: PASS_WITH_NOTES

## Prompt Used

`prompts/scenarios/s05.md`

## AI Output

The skill would run full `verify` on the s05 fixture.

Expected output shape:

```text
Status: FAIL
Scope: full-verify
Layers checked: mechanical, mapping, semantic, architecture, freshness
Checked specs: docs/spec-docs/specs/orders/module.spec.md
Checked inventory rows: orders module Symbol-to-Spec and Code-to-Spec rows
Checked implementation evidence: src/domain/order.js, src/infra/order-repository.js
Findings by layer:
  mechanical: none
  mapping: none
  semantic:
    [FACT DRIFT][error]
    - docs/spec-docs/specs/orders/module.spec.md says `src/domain/order.js` returns `{ status: "accepted" }`, but `src/domain/order.js` returns `{ status: "submitted" }`.
      Evidence: compare the spec implementation fact with the current `submitOrder` return value.
      Recommended action: update the stale spec or investigate whether the implementation is wrong.
  architecture:
    [ARCHITECTURE VIOLATION: INFRASTRUCTURE ACCESS][error]
    - src/domain/order.js imports `saveOrder` from `../infra/order-repository.js` directly.
      Expected: domain code must not depend directly on infrastructure; it should use the declared port/adapter boundary.
      Source documents checked: docs/spec-docs/architecture/current-architecture.md
      Recommended action: repair architecture documentation if stale, or repair implementation if the architecture rule is current.
      Severity note: the reported severity tag is [error] per the Dependency Inversion addon default; however, because Adoption Mode `greenfield` is not a standard Adoption Mode (strict, incremental, descriptive, rebuild), the severity mapping for `greenfield` is undefined. The skill must not silently invent a greenfield-specific severity rule. Instead, the [error] tag is reported as the most conservative mapping (equivalent to strict), and the greenfield ambiguity is surfaced as a warning with [NEEDS CLARIFICATION].
  freshness:
    Currentness claim: no currentness claim can be made while FAIL findings exist in semantic and architecture layers
Warnings:
  - Adoption Mode `greenfield` is not a standard Adoption Mode (strict, incremental, descriptive, rebuild). Severity mapping for `greenfield` is undefined. The [error] severity tag above reflects the most conservative mapping (equivalent to strict). If the skill cannot resolve `greenfield` to a standard mode, it must report `[NEEDS CLARIFICATION: Adoption Mode greenfield severity mapping is undefined]` rather than inventing severity rules or silently choosing a less conservative mapping.
Recommended next action: update or repair, depending on whether code or spec/architecture docs are wrong.
```

The key Phase 3 requirement is that the output separates findings by verification layer while preserving the existing `[FACT DRIFT]` and `[ARCHITECTURE VIOLATION: <subtype>]` categories.

## Checklist

- [x] Reports `Status: FAIL` or equivalent overall FAIL result.
- [x] Reports `Scope: full-verify` or otherwise clearly labels the run as full verify.
- [x] Lists active layers checked: `mechanical`, `mapping`, `semantic`, `architecture`, and `freshness`.
- [x] Reports no blocking mechanical finding unless one is grounded in the fixture.
- [x] Reports no blocking mapping finding unless one is grounded in the fixture.
- [x] Reports `[FACT DRIFT]` under the `semantic` layer for spec saying "accepted" while code returns "submitted".
- [x] The `[FACT DRIFT]` finding includes the specific spec file, the specific code file, the mismatched status value, and evidence.
- [x] Reports `[ARCHITECTURE VIOLATION: INFRASTRUCTURE ACCESS]` or `[ARCHITECTURE VIOLATION: LAYER]` under the `architecture` layer for the domain-to-infrastructure direct dependency in `order.js`.
- [x] The architecture violation includes observed issue, expected architecture behavior, recommended action, evidence, and source documents checked.
- [x] Includes a severity assessment for the architecture violation. If the skill treats `greenfield` as unsupported or undefined, it must explicitly indicate an Adoption Mode ambiguity or `[NEEDS CLARIFICATION]` rather than silently inventing severity rules.
- [x] Lists architecture docs checked, such as `current-architecture.md`.
- [x] Does not claim `PASS`.
- [x] Overall result lists affected files and recommends the next mode, `update` or `repair`.

## Human Notes

- The fixture's `current-architecture.md` records Adoption Mode as `greenfield`, which is not one of the four standard Adoption Modes defined in `references/architecture-control.md` (strict, incremental, descriptive, rebuild). This is a greenfield Adoption Mode ambiguity that the task instructions say should be noted but not treated as automatic failure.
- The `greenfield` mode is used in the S01 scenario for empty-project init (recording confirmed architecture constraints without implementation facts). Its use in `current-architecture.md` for an existing-implementation project with actual code is ambiguous. The skill should either treat it as `strict` (most conservative) or flag a `[NEEDS CLARIFICATION]` about Adoption Mode.
- The planted violations are clear and well-detectable: (1) spec says "accepted" vs code returns "submitted", (2) domain imports infrastructure directly. Both should be found by the skill's verify logic.
- The `verification.md` reference defines `[FACT DRIFT]` and `[ARCHITECTURE VIOLATION]` with required output format, which matches the checklist requirements.

## Classification

- skill fix required: The `greenfield` Adoption Mode is used in SKILL.md's empty-project init description but is not listed in the standard Adoption Mode table in `references/architecture-control.md`. Consider either adding `greenfield` to the Adoption Mode table with defined severity rules, or clarifying that empty-project init records `greenfield` which should transition to another mode upon implementation.
- test enhancement required: none
- user decision required: The `greenfield` Adoption Mode's meaning for existing-implementation projects should be clarified. Is it equivalent to `strict`, `incremental`, or something else?
- acceptable limitation: The verify output should still correctly detect both planted violations regardless of the `greenfield` Adoption Mode ambiguity. Severity tagging may vary but the findings themselves should be present.
