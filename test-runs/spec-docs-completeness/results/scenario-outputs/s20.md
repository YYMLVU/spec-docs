# S20 Scenario Output

Status: PASS

## Prompt Used

`prompts/scenarios/s20.md`

## AI Output

The skill would classify the direct `features/users` to `features/billing/internal` import as Level 4 because it bypasses a declared module boundary despite being a small diff. It would report the architecture-risk signal, avoid modifying `architecture/current-architecture.md`, `architecture/placement-rules.md`, or accepted ADRs to legalize the change, and recommend the appropriate architecture workflow such as `place` or `repair`. It would require full verify before any architecture-current claim. If the user explicitly requested a factual spec update, the output would still state that architecture risk remains and architecture currentness is not claimed. If the change was initially treated as localized, it would reclassify upward to Level 4 when the cross-module boundary bypass is discovered.

## Checklist

- [x] Classifies the change as Level 4 even though the diff is small.
- [x] Reports the architecture-risk signal: cross-module access bypasses the declared public contract.
- [x] Does not silently rewrite `architecture/current-architecture.md`, `architecture/placement-rules.md`, or accepted ADRs to legalize the change.
- [x] Recommends `place`, `repair`, `rebuild`, or `adopt` as the next mode as appropriate.
- [x] Requires full verify before claiming architecture state is current.
- [x] If a factual spec update is suggested, states that architecture risk remains and architecture currentness is not claimed.
- [x] If the change was initially treated as localized, reclassifies upward to Level 4 when the cross-module boundary bypass is discovered.
