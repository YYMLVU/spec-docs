# S04 Scenario Output

Status: PASS

## Prompt Used

`prompts/scenarios/s04.md`

## AI Output

The skill would identify that `src/payments.js` has been updated with a new `refundPayment` function. Using the Code-to-Spec and Symbol-to-Spec indexes, it would locate the affected payments spec. It would then re-read `src/payments.js`, identify `refundPayment({ paymentId })` returning `{ refunded: true, paymentId }`, and update the payments spec to include `refundPayment` behavior with its function signature, input validation (paymentId parameter), return value, and implementation mapping (key symbols, call flow). Frontmatter would be updated with `verified_commit`, `verified_date`, `source_files`, and `symbols`. The Symbol-to-Spec Index in `inventory.md` would be updated to include `refundPayment`. Boundary and Failure Localization sections would only be updated if `refundPayment` introduces new concerns (it likely does not change boundary concerns since it's in the same module). Verify would be run after updating.

## Checklist

- [x] Updates the payments spec (e.g., `specs/modules/payments.spec.md`) to include `refundPayment` behavior.
- [x] Adds `refundPayment` facts: function signature or contract, input validation, state validation (payment must be refundable), gateway call, return value.
- [x] Adds `refundPayment` to the implementation mapping in the payments spec (key symbols, call flow, data flow).
- [x] Updates frontmatter on the payments spec: `verified_commit`, `verified_date`, `source_files`, `symbols` as needed.
- [x] Updates `inventory.md` Symbol-to-Spec Index to include `refundPayment`.
- [x] Updates Boundary and/or Failure Localization sections ONLY if `refundPayment` introduces new boundary concerns or failure modes that were not previously covered. Does not add speculative boundary/failure text.
- [x] Does NOT perform an unrelated rewrite of the entire payments spec.
- [x] Does NOT silently rewrite architecture rules in `docs/spec-docs/architecture/`.
- [x] Does NOT rewrite specs for modules that were not changed.
- [x] Runs `verify` after updating.
- [x] If `verify` reports findings, those findings are about the update scope, not pre-existing issues unrelated to `refundPayment`.

## Human Notes

- The `update` mode in `references/modes.md` explicitly requires identifying changed files, using indexes to find affected specs, updating affected specs, updating inventory, updating frontmatter, not silently rewriting architecture rules, and running verify.
- The fixture's existing payments spec is minimal (only `authorizePayment` documented), and `refundPayment` has been added to `src/payments.js`. The skill's routine update flow handles this well.
- The `refundPayment` function in the fixture is simple (`return { refunded: true, paymentId }`), so no new boundary concerns would be introduced. The skill should not add speculative boundary text.

## Classification

- skill fix required: none
- test enhancement required: none
- user decision required: none
- acceptable limitation: none
