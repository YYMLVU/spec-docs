# S09 Scenario Output

Status: PASS

## Prompt Used

`prompts/scenarios/s09.md`

## AI Output

The skill would run `diagnose` for the "duplicate order confirmation emails" symptom. It would read `docs/spec-docs/architecture/current-architecture.md` (Event-driven Architecture with Idempotency and Observability addons), `debugging-rules.md` (boundary-first diagnosis: check event idempotency and consumer retry behavior before modifying shared email utilities), and `docs/spec-docs/specs/order-events/module.spec.md` (which identifies the boundary, failure localization, first files to inspect, and "do not start by modifying" guidance).

The diagnosis would identify the likely owner as the order-events module (publishing OrderConfirmed events), the likely failure boundary as the event publishing/consumption boundary (event idempotency or consumer retry), list first specs/files to inspect (`src/events/order-confirmed.js`, `src/consumers/send-confirmation-email.js`), list signals to check (event publishing logs, consumer retry logs, idempotency keys, duplicate event detection), include a "Do Not Start Here" section (shared email utilities, SMTP configuration, email template rendering), and suggest a debugging order following architecture boundaries (event contracts, then publishing, then consumption, then delivery). Possible fix areas would be presented as hypotheses, not confirmed solutions.

## Checklist

- [x] Identifies likely owner or marks owner unclear (order-events module exists but owner may not be explicit).
- [x] Identifies a likely failure boundary -- the boundary where the issue is most likely to originate (e.g., event publishing vs consumption boundary).
- [x] References debugging rules from `docs/spec-docs/architecture/debugging-rules.md`.
- [x] References module failure-localization information from relevant module specs (`docs/spec-docs/specs/order-events/module.spec.md`).
- [x] Lists first specs/files to inspect -- e.g., order-events module spec, event contracts, `src/events/order-confirmed.js`, `src/consumers/send-confirmation-email.js`.
- [x] Lists logs/metrics/error signals to check -- e.g., order placement events, email send events, event publishing/consuming logs, idempotency keys, duplicate event detection logs.
- [x] Includes a "Do Not Start Here" section that explicitly lists areas that should NOT be investigated first (e.g., shared email utilities, SMTP configuration, email template rendering).
- [x] Does NOT claim a root cause without evidence.
- [x] Does NOT recommend modifying shared email utilities as the first investigation path without evidence.
- [x] Does NOT recommend modifying unrelated modules (e.g., billing, user profile) before investigating the order/notification event flow.
- [x] Suggests a debugging order that follows architecture boundaries: check event contracts first, then event publishing, then event consumption, then email delivery.
- [x] Possible fix areas are suggested as hypotheses requiring evidence, not as confirmed solutions.
- [x] Consumes architecture facts from `current-architecture.md` -- uses Event-driven Architecture with Idempotency and observability addons to guide debugging (checks event ownership, publisher/consumer contracts, idempotency, and consistency boundaries).
- [x] Output follows the diagnosis template format: symptom, likely ownership, likely failure boundary, first specs/files to inspect, logs/metrics/error signals to check, do not start here, suggested debugging order, possible fix areas.

## Human Notes

- The fixture is well-designed for this scenario with a `debugging-rules.md` that provides boundary-first diagnosis guidance, and a `module.spec.md` that includes failure localization with "Do Not Start By Modifying" guidance.
- The Event-driven Architecture with Idempotency addon is directly relevant to the duplicate email symptom -- idempotency failures (missing idempotency keys, consumer retries without idempotency checks) are a common cause of duplicate event processing.
- The `diagnose` mode in `references/architecture-control.md` and `templates/diagnosis.md` define the output format, which matches the checklist requirements.
- The skill's defined behavior is to not claim root cause without evidence, which is critical for this scenario.

## Classification

- skill fix required: none
- test enhancement required: none
- user decision required: none
- acceptable limitation: none
