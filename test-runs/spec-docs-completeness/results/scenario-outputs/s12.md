# S12 Scenario Output

Status: PASS

## Prompt Used

`prompts/scenarios/s12.md`

## AI Output

The skill would use adopt mode and read `docs/spec-docs/rebuild/status.md` first. Because the status says `Current Phase: ready-to-adopt` and `Last Verify Result: PASS`, this is a true completed-rebuild adopt scenario rather than a first-time `init` introduction.

The skill would verify the source files against the target architecture rules: catalog behavior is under `src/features/catalog/`, generic SKU formatting is under `src/shared/format.js`, feature code may import shared utilities, and shared utilities do not import feature modules. The fixture satisfies those rules.

Expected adopt actions:

- merge the target Feature Modular Architecture selection into `current-architecture.md`;
- preserve relevant placement rules;
- archive `target-architecture.md` and `rebuild/adoption-plan.md` or state the exact archive operation;
- update `rebuild/status.md` to completed;
- run or recommend verify after adopt;
- avoid treating this as first-time init.

## Checklist

- [x] Reads `docs/spec-docs/rebuild/status.md` and uses it as the source of truth.
- [x] Confirms the project is ready for true `adopt`, not first-time `init`.
- [x] Verifies source files match target architecture rules before adopting.
- [x] Merges target Architecture Selection into `current-architecture.md` or describes the exact merge operation.
- [x] Preserves placement rules relevant to the adopted target architecture.
- [x] Archives `target-architecture.md` and `adoption-plan.md` or describes the exact archive operation.
- [x] Marks rebuild completed or describes the exact status update.
- [x] Does not invent missing rebuild evidence.
- [x] Does not treat this as a first-time legacy project introduction.

## Human Notes

- This scenario closes F-003 for a completed-rebuild fixture. It complements S8, which covers real-project first-time introduction and gradual adoption.
- This is a prompt-based reasoning test. It does not claim that the skill actually mutated files unless a later live run is explicitly approved.

## Classification

- skill fix required: none
- test enhancement required: none
- user decision required: none
- acceptable limitation: This confirms expected adopt reasoning against a completed-rebuild fixture, not live file mutation.