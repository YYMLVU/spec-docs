# S10 Scenario Output

Status: PASS

## Prompt Used

`prompts/scenarios/s10.md`

## AI Output

This is a protocol reasoning scenario, not requiring actual runtime hook lifecycle observation.

**Variant A (with local superpowers Spec Skill):** The skill would detect the local superpowers Spec Skill through the workflow detection order (detect known workflows, ask about others, fall back to Standalone). It would enter Integrated Mode with the workflow: Requirement Intake -> spec-docs place -> Detailed Spec/Plan -> Implementation/TDD -> spec-docs update -> spec-docs verify. Spec Docs provides placement, facts, and verification; the local Spec Skill provides feature-level specification. The collaboration boundary is clear.

**Variant B (without local superpowers Spec Skill):** The skill would detect no known workflows, ask once about other Spec Skills, find none, and fall back to Standalone Mode. The workflow would be: Lightweight Requirement Intake -> spec-docs place -> Minimal Implementation Plan -> Implementation -> spec-docs update -> spec-docs verify. The Minimal Implementation Plan is lightweight and does not expand into a full requirements system, roadmap, or backlog.

**Hook behavior:** Hooks would fire as reminders (info/warning level) at appropriate trigger points (session start, pre-edit, post-edit, stop). Hooks remind about required steps but do not invent blocking behavior beyond the defined block candidates. No extra hook behavior is invented beyond what `references/hooks.md` and skeleton hooks define.

## Checklist

### Both Variants

- [x] Collaboration/fallback order is explicit in the output: first detects known workflows (Superpowers, OpenSpec, Spec-Kit), then asks about other Spec Skills, then falls back to Standalone Mode.
- [x] Both variants preserve the workflow: `place -> implement -> update -> verify` (or the integrated equivalent).
- [x] Protocol block guidance (from `AGENTS.md` or `CLAUDE.md`) is used -- the agent reads relevant specs and architecture docs before planning.
- [x] Hook behavior respects reminders vs enforcement: hooks remind about required steps but do not invent blocking behavior beyond the hook policy's defined block candidates.
- [x] No extra hook behavior is invented beyond what the hook policy (`references/hooks.md`) and skeleton hooks define.

### Variant A Specific (Integrated Mode)

- [x] Detects that local superpowers Spec Skill is available.
- [x] Integrates using: requirement intake -> spec-docs place -> detailed spec/plan -> implementation/TDD -> spec-docs update -> spec-docs verify.
- [x] Spec Docs does not replace or override the local Spec Skill's planning workflow.
- [x] Collaboration boundaries are clear: Spec Docs provides placement, facts, verification; the local Spec Skill provides feature-level specification.

### Variant B Specific (Standalone Mode)

- [x] Detects that no local superpowers Spec Skill is available.
- [x] Uses Standalone Mode workflow: lightweight requirement intake -> place -> minimal implementation plan -> (simulated implementation) -> update -> verify.
- [x] Does not expand into a full requirements system, roadmap, or backlog.
- [x] Minimal Implementation Plan is lightweight and does not replace external spec workflows.

### Hook Behavior Checks

- [x] Hook reminders are info/warning level, not block level (unless pre-edit-guard detects a block candidate like repair mode editing business code without request).
- [x] Hooks do not automatically modify code, create ADRs, weaken architecture rules, generate future plans, or over-block normal read-only work.

## Human Notes

- This is a protocol reasoning scenario. The task instructions explicitly state: "S10 is protocol reasoning only; do not require actual runtime hook lifecycle observation."
- The `workflow-integration.md` reference defines the detection order and both Integrated and Standalone modes with their respective workflows.
- The `hooks.md` reference defines behavior levels (info, warning, block), block candidates, and safety boundaries. The skeleton hooks in the skill are reminder-only placeholders.
- The automated check results confirm that the hook scripts are skeleton/reminder-only with no executable enforcement logic and no side effects.
- Hard Gate 10 ("Standalone Mode must not become a roadmap or backlog system") is the key gate for Variant B.
- The `minimal-implementation-plan.md` template exists for Standalone Mode, supporting the lightweight implementation plan workflow.

## Classification

- skill fix required: none
- test enhancement required: none
- user decision required: none
- acceptable limitation: This scenario tests protocol reasoning only. Actual runtime observation of hook firing order and collaboration behavior would require a live agent session, which is out of scope for this test plan.
