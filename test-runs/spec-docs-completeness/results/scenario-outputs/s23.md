# S23 Scenario Output

Status: PASS

## Prompt Used

`prompts/scenarios/s23.md`

## AI Output

The skill would route this as `place` scoped because the request is a bounded placement question for one renderer inside an existing notifications feature, current architecture references identify the owner, and no ADR or cross-boundary rule change is requested.

Expected scoped placement output:

- Feature intent: add an email notification template renderer for the existing notifications feature.
- Decision: place the renderer under the notifications feature, such as `src/features/notifications/`, using the project's existing internal/public split.
- Owning module: `notifications`.
- Layer placement: feature/application or presentation-adjacent renderer layer, depending on the project's current notifications layout.
- Allowed dependencies: notifications-owned helpers and stable shared formatting primitives if already allowed.
- Forbidden dependencies: imports from another feature's `internal/` files and direct infrastructure access unless routed through existing notifications contracts.
- Required public contracts: expose behavior through the notifications public API only if other features need to call it.
- Specs and architecture files to read: notifications spec, current architecture, placement rules.
- Specs to update after implementation: notifications spec and affected inventory rows if new files or exported symbols are added.
- Confidence: high, because owner and boundary are explicit.

The output would not modify code, create implementation specs before implementation, recommend rebuild/full repair/full adopt, or claim architecture currentness.

## Checklist

- [x] Routes to `place` scoped.
- [x] Uses the smallest needed architecture references.
- [x] Gives owning module and layer placement.
- [x] Lists allowed and forbidden dependencies.
- [x] Lists required public contract guidance.
- [x] Lists specs/files to read and specs to update after implementation.
- [x] Does not modify code.
- [x] Does not create implementation specs as if behavior already exists.
- [x] Does not recommend rebuild, full repair, or full adopt.
- [x] Does not claim architecture currentness.
- [x] Would escalate if ownership, dependency direction, public contract, or ADR relevance became unclear.

## Classification

- skill fix required: none
- test enhancement required: none
- user decision required: none
- acceptable limitation: none
