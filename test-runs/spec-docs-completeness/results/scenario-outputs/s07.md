# S07 Scenario Output

Status: PASS

## Prompt Used

`prompts/scenarios/s07.md`

## AI Output

The skill would run `rebuild` on the s07 fixture. It would read `docs/spec-docs/rebuild/status.md` and determine the current phase is `target-architecture-draft`. It would NOT infer phase from the existence of `target-architecture.md` (Hard Gate 9). The status file says "Current Phase: target-architecture-draft" and "Next: Draft target architecture." Since `target-architecture.md` exists but contains only a note that it must not be used alone to determine rebuild phase, the skill would determine that the draft target architecture needs to be written or confirmed. It would keep current architecture and target architecture separate. It would treat Adoption Mode as `rebuild` for the rebuild lifecycle. If advancing phase, it would update `rebuild/status.md` accordingly. It would not modify current architecture rules to match target architecture prematurely.

## Checklist

- [x] Reads `docs/spec-docs/rebuild/status.md` to determine the current rebuild phase.
- [x] Determines phase from `status.md` content (`Current Phase: target-architecture-draft`), NOT the mere existence of `target-architecture.md`, to decide what actions are available.
- [x] Does not infer phase from `target-architecture.md` existence (Hard Gate 9).
- [x] Keeps current architecture and target architecture (`target-architecture.md`) separate and distinct.
- [x] Treats Adoption Mode as `rebuild` for the rebuild lifecycle.
- [x] If advancing phase (e.g., from target-architecture-draft to next phase), updates `rebuild/status.md` accordingly.
- [x] During active rebuild, `place` and `verify` consideration of target architecture is explicitly stated.
- [x] Does not modify current architecture rules to match target architecture prematurely.
- [x] Rebuild progression is tracked with clear phase transitions and evidence.
- [x] ADR rationale for the rebuild choice is referenced or confirmed.
- [x] If the rebuild phase cannot advance (e.g., code has not been migrated yet), does not force advancement.

## Human Notes

- Hard Gate 9 ("Rebuild mode is determined by `rebuild/status.md`, not by target file existence") is the critical gate for this scenario. The fixture explicitly tests this by making `target-architecture.md` exist but `status.md` being the authoritative source.
- The `rebuild` mode in `references/modes.md` and `references/architecture-control.md` defines the rebuild lifecycle with clear phase tracking.
- The fixture's `status.md` indicates the current phase is `target-architecture-draft` with the next step being to draft the target architecture. This is a transitional state where the skill should help draft/confirm the target architecture before advancing.
- The `rebuild-status.md` template provides the format for tracking rebuild progress.

## Classification

- skill fix required: none
- test enhancement required: none
- user decision required: none
- acceptable limitation: none
