# S24 Scenario Output

Status: PASS

## Prompt Used

`prompts/scenarios/s24.md`

## AI Output

The skill would route this as `repair` scoped because the stale documentation is limited to one orders spec section, the governing architecture rule is current and explicit, and the implementation already follows the port boundary.

Expected scoped repair actions:

- Re-read `src/domain/order.js`, the orders spec, `architecture/current-architecture.md`, and `architecture/placement-rules.md`.
- Replace the stale statement that domain code may call `src/infra/order-repository.js` directly with the current fact that domain code uses `OrderRepositoryPort`.
- Update affected frontmatter fields such as `verified_commit`, `verified_date`, `source_files`, and `symbols` if the spec evidence changes.
- Update inventory mappings only if source or symbol mappings changed.
- Do not modify business code.
- Do not weaken architecture rules or accepted ADRs.
- Run full verify before claiming repair complete or architecture currentness.

## Checklist

- [x] Routes to `repair` scoped.
- [x] Grounds repair in current implementation and architecture references.
- [x] Updates only the localized stale spec content and affected metadata.
- [x] Does not modify business code.
- [x] Does not weaken architecture rules or accepted ADRs.
- [x] Preserves full verify requirement before repair completion.
- [x] Does not claim architecture currentness before full verify.
- [x] Would escalate if broader drift, ADR impact, or stale architecture rules were discovered.

## Classification

- skill fix required: none
- test enhancement required: none
- user decision required: none
- acceptable limitation: none
