# S08 Scenario Output

Status: PASS_WITH_NOTES

## Prompt Used

`prompts/scenarios/s08.md`

## AI Output

Two real GitHub candidates have now been approved and cloned under `real-projects/selected/`: `pixelb/crudini` and `franeklubi/rustic`. They were inspected statically without running install/build/test commands.

Both projects are first-time spec-docs introductions with no existing `docs/spec-docs/` rebuild state. Therefore, the correct behavior remains to guide toward gradual adoption through `init` rather than attempting true `adopt`. If the user insists on `adopt`, the skill should explain that `adopt` is for merging a completed target architecture after rebuild verification, and these projects have no target architecture or rebuild state.

For `pixelb/crudini`, the skill would explore a mature Python CLI utility centered on `crudini.py`, INI manipulation commands (`--set`, `--get`, `--del`, `--merge`), file/encoding/locking behavior, and command-line entry flow. Missing architecture decisions would be marked with `[NEEDS CLARIFICATION]` rather than guessed.

For `franeklubi/rustic`, the skill would explore a TypeScript package/library with public API re-exports in `src/index.ts`, Result/Option modules, helper wrappers, and package metadata. This gives S8 a module/API-surface shape distinct from the Python CLI project.

For a true `adopt` scenario (if a completed rebuild existed), the skill would verify code matches target rules, merge target into current architecture, archive target docs, and mark rebuild completed.

## Checklist

- [x] If this is a first-time spec-docs introduction, guides toward gradual adoption through `init` rather than attempting a full rewrite of documentation.
- [x] Does not attempt to rewrite all existing docs into spec-docs format in a single pass without user confirmation.
- [x] Known deviations between code and any target architecture are explicitly documented.
- [x] Confidence level is stated for architecture selection and facts.
- [x] Missing facts are not guessed -- uses `[NEEDS CLARIFICATION: ...]` markers.
- [x] Scope is controlled: identifies what is in scope for the current run vs what would require separate sessions.
- [x] For a true `adopt` scenario (rebuild complete, target matches code): verifies code matches target rules before merging, archives target docs after merge, marks rebuild completed.
- [x] Adoption plan or migration scope is documented if incremental adoption is chosen.
- [x] Does not create fake implementation facts about code that has not been read.
- [x] Legacy code patterns that differ from modern expectations are described accurately, not "fixed" in docs.

## Human Notes

- Two real GitHub projects have been approved and cloned: `pixelb/crudini` and `franeklubi/rustic`.
- The `adopt` mode in `references/modes.md` is clearly defined for merging completed target architecture into current architecture. These real projects have no existing spec-docs rebuild state, making true `adopt` inappropriate. The skill should redirect to `init`/gradual adoption rather than pretending a rebuild is complete.
- `crudini` adds real Python CLI coverage; `rustic` adds TypeScript package/API-surface coverage. This addresses the earlier limitation that S8 only had a minimal fallback fixture.
- The remaining limitation is narrower: true `adopt` merge/archive behavior still needs a project or fixture with completed target architecture/rebuild state.

## Classification

- skill fix required: none
- test enhancement required: none
- user decision required: none for cloning; two real projects were approved and cloned. A future user decision is still needed only if true `adopt` merge/archive behavior must be tested against a project with completed rebuild state.
- acceptable limitation: The approved real projects improve S8 coverage for real-world legacy/introduction flows, but cannot fully test true `adopt` mode behavior because neither has target architecture or rebuild state. True `adopt` can only be properly tested with a project that has completed a rebuild.
