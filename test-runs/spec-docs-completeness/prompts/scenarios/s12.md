# S12 True Adopt Completed Rebuild Prompt

Working directory: `test-runs/spec-docs-completeness/fixtures/s12-adopt-completed-rebuild/`

Use the spec-docs skill in adopt mode.

This fixture represents a completed rebuild. `docs/spec-docs/rebuild/status.md` says `Current Phase: ready-to-adopt` and `Last Verify Result: PASS`. Source files already match the target Feature Modular Architecture.

Expected checklist:

- [ ] Reads `docs/spec-docs/rebuild/status.md` and uses it as the source of truth.
- [ ] Confirms the project is ready for true `adopt`, not first-time `init`.
- [ ] Verifies source files match target architecture rules before adopting.
- [ ] Merges target Architecture Selection into `current-architecture.md` or describes the exact merge operation.
- [ ] Preserves placement rules relevant to the adopted target architecture.
- [ ] Archives `target-architecture.md` and `adoption-plan.md` or describes the exact archive operation.
- [ ] Marks rebuild completed or describes the exact status update.
- [ ] Does not invent missing rebuild evidence.
- [ ] Does not treat this as a first-time legacy project introduction.

Result: PASS / PASS_WITH_NOTES / FAIL / BLOCKED
