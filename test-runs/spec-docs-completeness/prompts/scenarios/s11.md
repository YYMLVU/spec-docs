# S11 Architecture Drift Verify Prompt

Working directory: `test-runs/spec-docs-completeness/fixtures/s11-verify-architecture-drift/`

Use the spec-docs skill in verify mode.

The project uses Feature Modular Architecture with Strong Module Boundaries and strict Adoption Mode. Billing-specific policy belongs in `src/features/billing/`. Shared utilities must remain generic.

Expected checklist:

- [ ] Reads `docs/spec-docs/architecture/current-architecture.md` and `placement-rules.md`.
- [ ] Reads `docs/spec-docs/specs/billing/module.spec.md`.
- [ ] Inspects `src/shared/utils.js` and identifies `discountForBillingTier(tier)` as billing-specific business logic in shared code.
- [ ] Reports `[ARCHITECTURE VIOLATION: ARCHITECTURE DRIFT]`.
- [ ] Explains that `src/shared/` is accumulating feature-specific billing policy.
- [ ] Includes evidence and affected files.
- [ ] Includes severity appropriate for strict Adoption Mode and Strong Module Boundaries.
- [ ] Does not automatically move code or repair the violation.
- [ ] Does not misclassify this only as generic fact drift.

Result: PASS / PASS_WITH_NOTES / FAIL / BLOCKED
