# S21 - Layered Verify Scope Without Currentness Overclaim

## Prompt

In the s21 fixture, the user asks:

> Run only a mechanical and mapping verification pass for the changed refunds spec. Do not run full verify.

The fixture represents:

- `docs/spec-docs/specs/payments/refunds.spec.md` exists and has valid frontmatter.
- `docs/spec-docs/inventory.md` maps `src/payments/refunds.js` to `docs/spec-docs/specs/payments/refunds.spec.md`.
- The referenced source file exists.
- No behavior or architecture review was requested.

Run the scoped verify request.

## Checklist

Verify the output:

- [ ] Reports `Status: PASS` or `PASS WITH WARNINGS` for the scoped check only.
- [ ] Reports `Scope: layered-check`.
- [ ] Lists `Layers checked: mechanical, mapping`.
- [ ] Does not list `semantic`, `architecture`, or `freshness` as checked layers.
- [ ] Confirms valid frontmatter and absence of unresolved placeholders for the checked spec.
- [ ] Confirms inventory/source/spec references resolve for the checked mapping.
- [ ] Does not claim full `verify` PASS.
- [ ] Does not claim semantic currentness, architecture currentness, release freshness, or workspace currentness.
- [ ] Uses `Currentness claim:` to state the bounded claim or `none`.
- [ ] Recommends full verify if the user wants semantic, architecture, release, or workspace currentness.

Result: PASS / PASS_WITH_NOTES / FAIL / BLOCKED
