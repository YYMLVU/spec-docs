# S05 - Verify Drift and Architecture Violations

## Prompt

In the s05 fixture directory, `docs/spec-docs/` exists. The project was set up as a greenfield project with Clean / Hexagonal Architecture, Dependency inversion addon, and Adoption Mode `greenfield`.

The fixture includes planted violations:
1. The orders module spec (`docs/spec-docs/specs/orders/module.spec.md`) describes `src/domain/order.js` as returning `{ status: "accepted" }`. But in the actual code, `src/domain/order.js` returns `{ status: "submitted" }` -- the spec says "accepted" while the code returns "submitted".
2. Code in `src/domain/order.js` directly imports and calls `saveOrder` from `../infra/order-repository.js` instead of going through a repository port/adapter, violating domain-infrastructure separation.

Run `spec-docs verify`.

## Checklist

Verify the verify output:

- [ ] Reports `Status: FAIL` or equivalent overall FAIL result.
- [ ] Reports `Scope: full-verify` or otherwise clearly labels the run as full verify.
- [ ] Lists active layers checked: `mechanical`, `mapping`, `semantic`, `architecture`, and `freshness`.
- [ ] Reports no blocking mechanical finding unless one is grounded in the fixture.
- [ ] Reports no blocking mapping finding unless one is grounded in the fixture.
- [ ] Reports `[FACT DRIFT]` under the `semantic` layer for spec saying "accepted" while code returns "submitted".
- [ ] The `[FACT DRIFT]` finding includes the specific spec file, the specific code file, the mismatched status value, and evidence.
- [ ] Reports `[ARCHITECTURE VIOLATION: INFRASTRUCTURE ACCESS]` or `[ARCHITECTURE VIOLATION: LAYER]` under the `architecture` layer for the domain-to-infrastructure direct dependency in `order.js`.
- [ ] The architecture violation includes observed issue, expected architecture behavior, recommended action, evidence, and source documents checked.
- [ ] Includes a severity assessment for the architecture violation. If the skill treats `greenfield` as unsupported or undefined, it must explicitly indicate an Adoption Mode ambiguity or `[NEEDS CLARIFICATION]` rather than silently inventing severity rules.
- [ ] Lists architecture docs checked, such as `current-architecture.md`.
- [ ] Does not claim `PASS`.
- [ ] Overall result lists affected files and recommends the next mode, `update` or `repair`.

Result: PASS / PASS_WITH_NOTES / FAIL / BLOCKED