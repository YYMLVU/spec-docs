# S19 - Level 3 Full Update and Full Verify

## Prompt

In the fixture project, a cross-cutting request ID behavior changed across API, job, and logging specs. Multiple ownership areas and multiple inventory sections are affected. No architecture rule or ADR is being changed.

Run `spec-docs update` and classify the impact.

## Checklist

Verify the update output:

- [ ] Classifies the change as Level 3 because multiple ownership areas and inventory sections are affected.
- [ ] Updates all affected specs, not only one mapped spec.
- [ ] Updates inventory mappings for the affected broad scope.
- [ ] Runs full verify before claiming the broad scope is current.
- [ ] Does not route to Level 4 when no architecture-risk signal is present.
- [ ] Does not claim release-level freshness beyond the checked broad scope unless explicitly requested.

Result: PASS / PASS_WITH_NOTES / FAIL / BLOCKED
