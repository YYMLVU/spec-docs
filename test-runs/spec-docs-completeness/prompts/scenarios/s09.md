# S09 - Architecture-Guided Diagnose

## Prompt

In the s09 fixture directory, `docs/spec-docs/` exists. The project uses Event-driven Architecture with Idempotency and observability addons.

The symptom to diagnose: **Users are receiving duplicate order confirmation emails.** When an order is placed, the confirmation email is sometimes sent twice. This started happening after a recent deployment that touched both the order module and the notification module.

Run `spec-docs diagnose` for the duplicate order confirmation email symptom.

## Checklist

Verify the diagnosis output:

- [ ] Identifies likely owner or marks owner unclear (order-events module exists but owner may not be explicit).
- [ ] Identifies a likely failure boundary -- the boundary where the issue is most likely to originate (e.g., event publishing vs consumption boundary).
- [ ] References debugging rules from `docs/spec-docs/architecture/debugging-rules.md`.
- [ ] References module failure-localization information from relevant module specs (`docs/spec-docs/specs/order-events/module.spec.md`).
- [ ] Lists first specs/files to inspect -- e.g., order-events module spec, event contracts, `src/events/order-confirmed.js`, `src/consumers/send-confirmation-email.js`.
- [ ] Lists logs/metrics/error signals to check -- e.g., order placement events, email send events, event publishing/consuming logs, idempotency keys, duplicate event detection logs.
- [ ] Includes a "Do Not Start Here" section that explicitly lists areas that should NOT be investigated first (e.g., shared email utilities, SMTP configuration, email template rendering).
- [ ] Does NOT claim a root cause without evidence.
- [ ] Does NOT recommend modifying shared email utilities as the first investigation path without evidence.
- [ ] Does NOT recommend modifying unrelated modules (e.g., billing, user profile) before investigating the order/notification event flow.
- [ ] Suggests a debugging order that follows architecture boundaries: check event contracts first, then event publishing, then event consumption, then email delivery.
- [ ] Possible fix areas are suggested as hypotheses requiring evidence, not as confirmed solutions.
- [ ] Consumes architecture facts from `current-architecture.md` -- uses Event-driven Architecture with Idempotency and observability addons to guide debugging (checks event ownership, publisher/consumer contracts, idempotency, and consistency boundaries).
- [ ] Output follows the diagnosis template format: symptom, likely ownership, likely failure boundary, first specs/files to inspect, logs/metrics/error signals to check, do not start here, suggested debugging order, possible fix areas.

Result: PASS / PASS_WITH_NOTES / FAIL / BLOCKED