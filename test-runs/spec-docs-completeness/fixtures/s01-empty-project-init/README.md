# S1 Empty Project Init Fixture

This directory intentionally contains no implementation files. It tests that `spec-docs init` does not invent implementation facts and still handles Architecture Selection.

Architecture input variant A: user says the intended architecture is Lightweight Tooling Architecture with no addons and greenfield adoption.

Architecture input variant B: user provides no architecture choice. Expected behavior is `[NEEDS CLARIFICATION]` or equivalent unconfirmed architecture fields, not guessing.