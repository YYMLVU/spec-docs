# S18 Level 2 Targeted Light Check Fixture

This directory contains a users interface where one endpoint response contract changed, requiring a targeted inventory mapping update. It tests that `spec-docs update` classifies the change as Level 2, runs targeted light check, and does not claim full verify PASS.
