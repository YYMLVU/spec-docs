# S19 Level 3 Full Update and Full Verify Fixture

This directory contains a cross-cutting request ID behavior change across API, job, and logging specs. It tests that `spec-docs update` classifies the change as Level 3 and runs full verify before claiming the broad scope is current.
