# S14 Large Phased Init Fixture

This fixture represents a larger application surface for reasoning tests. It intentionally contains several feature areas, runtime code, and integration code so `spec-docs init` can choose a phased init instead of producing shallow full coverage in one pass.
