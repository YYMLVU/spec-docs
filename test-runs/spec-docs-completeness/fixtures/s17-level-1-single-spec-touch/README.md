# S17 Level 1 Single-Spec Touch Fixture

This directory contains a mapped parser module where one private edge-case behavior changed. It tests that `spec-docs update` classifies the change as Level 1 and updates only the single affected mapped spec without full verify.
