# Hard Gates Reference

This file indexes hard-gate canonical homes. It does not duplicate full gate text.

## Canonical Homes

- Core hard gates: `SKILL.md` → `## Core Hard Gates`.
- Architecture hard gates: `references/architecture-control.md` → `## Architecture Hard Gates`.
- Verify hard gates: `references/verification.md`.
- Spec authoring hard gates: `references/spec-authoring.md`.
- Workflow hard gates: `references/workflow-integration.md`.
- Hook safety hard gates: `references/hooks.md`.

When gates appear to conflict, apply the stricter rule and report the conflict instead of weakening a gate silently.
