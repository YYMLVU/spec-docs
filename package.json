{
  "name": "spec-docs",
  "version": "1.0.2",
  "description": "Implementation-first AI spec knowledge base skill for Claude Code",
  "bin": {
    "spec-docs": "./bin/spec-docs.js"
  },
  "scripts": {
    "test": "node --check bin/spec-docs.js && ROOT=$PWD; tmp=$(mktemp -d); (cd \"$tmp\" && node \"$ROOT/bin/spec-docs.js\" >/dev/null); test -f \"$tmp/.claude/skills/spec-docs/SKILL.md\"; rm -rf \"$tmp\""
  },
  "files": [
    "skills/",
    "bin/",
    "agents/"
  ],
  "keywords": [
    "claude-code",
    "skill",
    "spec",
    "documentation",
    "ai"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/YYMLVU/spec-docs.git"
  }
}
